import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.construpro.construpro',
  appName: 'construpro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
